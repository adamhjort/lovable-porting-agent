interface:
  display_name: "Port Lovable App"
  short_description: "Port Lovable apps across independent targets"
  default_prompt: "Use $port-lovable-app to inventory this Lovable repository, select a supported target, and produce a safe dry-run migration plan."
